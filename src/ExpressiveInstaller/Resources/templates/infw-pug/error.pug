extends ../layout/layout

block title
    |#{status} #{reason}

block content
    h1 Oops!
    h2 This is awkward.
    p We encountered a #{ status } #{ reason } error.
    if status == 404
        p
            |You are looking for something that doesn't exist or may have moved. Check out one of the links on this page
            |or head back to
            a(href="/") Home.
